# Local CA Certificates (Do Not Commit)

Place your organization CA certificate files here when developing in a DPI-inspected network.

- Accepted extensions: `.crt`, `.pem`
- These files are ignored by git
- Certificates are registered automatically when the devcontainer starts

Example filenames:
- `corp-root-ca.crt`
- `corp-proxy-ca.pem`
